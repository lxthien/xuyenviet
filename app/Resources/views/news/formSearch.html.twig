<form action="{{ path('news_search') }}" method="get" id="form-search">
    <div class="form-group">
        <input name="q" type="text" class="form-control search-field" placeholder="{{ 'search'|trans }}" autocomplete="off">
    </div>
</form>