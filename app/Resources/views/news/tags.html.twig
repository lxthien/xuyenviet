{% extends 'layout.html.twig' %}

{% block title %}
    {{tag.getPageTitle() ~ ' - Page ' ~ pagination.currentPageNumber ~ ' of ' ~ pagination.pageCount }}
{% endblock %}
{% block description %}
    {{tag.getPageDescription()}}
{% endblock %}
{% block keyword %}
    {{tag.getPageKeyword()}}
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-8">
                <h1>{{ tag.getName() }}</h1>
                
                <div class="description">
                    <p>{{ tag.getContents()|raw }}</p>
                </div>

                {# List item on this page #}
                {% if pagination|length > 0  %}
                    {# Sort #}
                    {% if get_setting('isShowSortOnCategory') %}
                        {{ include('news/components/sort.html.twig') }}
                    {% endif %}

                    <div class="col-md-12 list-news-items">
                        <div class="row">
                            {% for row in objectOnHomepage.posts %}
                                {{ include('news/components/itemsGridHomepage.html.twig') }}
                            {% endfor %}
                        </div>
                    </div>

                    {# Pagination #}
                    {{ include('news/components/pagination.html.twig') }}
                {% else %}
                    <div class="no-record">
                        <p>{{ 'Do not have item on this page'|trans }}</p>
                    </div>
                {% endif %}
            </div>
            
            {# Side bar #}
            {{ include('layout/sidebar.html.twig') }}
        </div>
    </div>
{% endblock %}