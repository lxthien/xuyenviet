<div id="nav">
    <div class="nav-container">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <nav class="navbar navbar-default">
                        <div class="navbar-header">
                            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse" aria-expanded="false">
                                <span class="sr-only">Toggle navigation</span>
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                            </button>

                            <a class="navbar-brand hidden-xs hidden-sm hidden-md hidden-lg" href="{{ path('homepage') }}" title="{{ get_setting('siteName') }}">
                                <img src="{{ asset('assets/images/logo-mobile.png') }}" class="d-inline-block" alt="{{ get_setting('siteName') }}">
                            </a>
                        </div>

                        <div class="collapse navbar-collapse" id="navbar-collapse">
                            {{ knp_menu_render('AppBundle:Builder:mainMenu', {
                                'allow_safe_labels': true,
                            })|raw }}

                            <form class="navbar-form navbar-right" action="{{ path('news_search') }}" method="get">
                                <div class="form-group">
                                    <input name="q" type="text" class="form-control" placeholder="{{ 'search'|trans }}">
                                </div>
                            </form>
                        </div>
                    </nav>
                </div>
            </div>
        </div>
    </div>
</div>